
// Questo file JS è un placeholder. 
// Quando impacchettiamo un'app React buildata, il codice finale viene inserito nel bundle JS.
// Per questo esempio, il codice React è stato preparato e documentato, ma non compilato qui.
// Per usare davvero questo Add-In React, serve fare 'npm run build' e usare il file buildato.

document.addEventListener("DOMContentLoaded", () => {
  document.getElementById("root").innerHTML = `
    <h2>Questo è un placeholder JavaScript.</h2>
    <p>Per avere l'app React funzionante, compila con 'npm run build' e inserisci il bundle qui.</p>
  `;
});
